In order to run tests locally:

1) Install mocha globally on your PC 
	In FrontEnd/Website - npm install mocha -g
2) In the same folder enter "npm test" in the command line
3) CTRL ^ C to end the tests